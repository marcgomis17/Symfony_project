{% extends "layouts/dashboard.html.twig" %}

{% block title %}
{{ current_page}}
{% endblock %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('styles/liste-classes.css') }}">
{% endblock %}

{% block view %}

<div id="list" class="view container">
    <div id="filter-add">
        <!--  <select name="filter-modules" id="filter-modules" class="form-control">
            <option value="">--- Selectionner Un Module ---</option>
        </select> -->
        <a href=""><button type="button" class="btn btn-primary add-btn">+ Ajouter</button></a>
    </div>
    <div id="table-wrapper">
        <h2>Liste des modules</h2>
        <table class="table table-hover">
            <thead>
                <tr>
                    <th scope="col">N°</th>
                    <th scope="col">Nom Module</th>
                </tr>
            </thead>
            <tbody>
                {% for key, module in modules %}
                <tr>
                    <th scope="row">{{ key + 1 }}</th>
                    <td>{{ module.nomModule }}</td>
                    <td class="actions">
                        <form action="" method="post">
                            <input type="hidden" name="id" value="{{ module.id }}">
                            <button class="btn btn-primary" type="submit" data-id="{{ module.id }}" class="action-btn"><span><img class="action-icon" src="{{ asset('images/icons/edit.svg') }}"></span>Modifier</button>
                        </form>
                        <form action="" method="post">
                            <input type="hidden" name="id" value="{{ module.id }}">
                            <button class="btn btn-danger" type="submit" data-id="{{ module.id }}" class="action-btn"><span><img class="action-icon" src="{{ asset('images/icons/trash-fill.svg') }}"></span>Supprimer</button></a>
                        </form>
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    <div class="navigation">
        {{ knp_pagination_render(modules, '@KnpPaginator/Pagination/bootstrap_v5_pagination.html.twig') }}
    </div>
</div>

{% endblock %}